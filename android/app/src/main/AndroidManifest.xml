<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--
      In-app updater (v1.1.17): we sideload a newer APK from the
      GitHub Release page directly. REQUEST_INSTALL_PACKAGES is the
      manifest hook; the actual user grant happens via
      Settings → "Install unknown apps" the first time. Subsequent
      installs are silent because Android remembers the grant.
      ACCESS_NETWORK_STATE (declared by :feature:updater's own
      manifest, merged in here) gates the auto-download-on-Wi-Fi
      check.
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".TermxApplication"
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Termx"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Termx"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.TermxForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".notification.ApprovalActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="dev.kuch.termx.action.TERMX_APPROVE" />
                <action android:name="dev.kuch.termx.action.TERMX_DENY" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notification.ReconnectActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="dev.kuch.termx.action.TERMX_RECONNECT" />
            </intent-filter>
        </receiver>

        <!--
          FileProvider for the in-app updater (v1.1.17). Hands out
          temporary read URIs for the downloaded APK to Android's
          system installer Activity, which is the only consumer.
          Authority is namespaced under our applicationId so multiple
          apps could run side by side without colliding.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
