{
  "name": "termx-tooling",
  "private": true,
  "version": "1.1.14",
  "description": "Monorepo tooling for termx (Android SSH client designed for commanding Claude Code on a VPS)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "release:android": "GITHUB_TOKEN=$(gh auth token) release-it",
    "release:termxd": "cd termxd && GITHUB_TOKEN=$(gh auth token) release-it --config ../.release-it.termxd.json"
  },
  "devDependencies": {
    "@release-it/conventional-changelog": "^10.0.5",
    "release-it": "^19.2.4"
  }
}
