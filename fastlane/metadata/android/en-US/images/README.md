# F-Droid placeholder images

The PNGs in `phoneScreenshots/` and `icon.png` are transparent stand-ins
generated by Task #49. **Replace before submission** — the F-Droid build
server expects real screenshots, and the icon should mirror the 512x512
adaptive-icon render from `android/app/src/main/res/mipmap-anydpi-v26/`
once the launcher art is finalised.

Checklist before flipping the repo public:

- [ ] Replace `icon.png` with a 512x512 PNG of the production launcher
  icon (export from Android Studio Asset Studio).
- [ ] Capture five phone screenshots at 1080x1920 covering:
  server list, setup wizard, terminal with Claude session, diff viewer,
  settings.
- [ ] Double-check there's no telemetry-like asset bundled by accident.
- [ ] Update `../changelogs/<versionCode>.txt` to match the final release.
